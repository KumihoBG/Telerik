namespace SquareRoot
{
    using System;
    class SquareRoot
    {
        static void Main()
        {
            // This is simple application showing the calculation of the square root of the number 12345;
            // Using the System.Math class, calling Sqrt method; using placeholder;
            double number = Math.Sqrt(12345);
            Console.WriteLine("The square root of the number 12345 is {0}", number);
        }
    }
}
